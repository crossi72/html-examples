document.addEventListener('DOMContentLoaded', filter_manager);

function filter_manager(){
	let button = document.querySelector("#btn-filter");

	button.addEventListener('click', filter_table);

	function filter_table(){

	}
}